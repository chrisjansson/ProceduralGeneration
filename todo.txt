cache value in uniform setters/dirty check in setters
add sources of dependencies so that they can be built if needed
read and render heightmap from file
shadows
gl debug extensions
refactor  makeShader to return Result discriminated union